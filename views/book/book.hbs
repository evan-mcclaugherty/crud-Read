{{#each book}}
  <section class="flex books">
    <img src="{{cover_url}}" />
    <div>
      {{title}}
      <br> {{description}}
      <br>
      <a href="/book/delete/{{id}}">
        <i class="fa fa-remove"></i>
      </a>
    </div>
  </section>
{{/each}}
<a href="/book/new">Add Book</a>
